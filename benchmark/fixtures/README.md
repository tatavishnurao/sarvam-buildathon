# Fixture notice

`mat_armstrong_*` files are **synthetic test inputs**, assembled only from the
acceptance-test facts in this task. They are not a transcription of a Mat
Armstrong video, a Telugu dub, or a benchmark result. Replace both with locally
provided assets before any real evaluation.
